
export const Breadcrumbs = () => {
  return (
    <div>Soy las migas de pan</div>
  );
};

/* 
import { useParams } from 'react-router-dom';

export const Breadcrumbs = () => {
  const { name, id } = useParams();
  return (
    <nav>
      {name && <span>Category: {name}</span>}
      {id && <span>Product ID: {id}</span>}
    </nav>
  );
};
*/
